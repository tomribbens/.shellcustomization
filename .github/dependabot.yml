version: 2
updates:
  # Keep the GitHub Actions used in workflows up to date (e.g. actions/checkout),
  # so runtime deprecations get an automatic PR instead of a manual bump.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
